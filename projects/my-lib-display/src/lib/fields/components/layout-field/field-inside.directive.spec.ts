import { FieldInsideDirective } from './field-inside.directive';

describe('FieldInsideDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldInsideDirective();
    expect(directive).toBeTruthy();
  });
});
